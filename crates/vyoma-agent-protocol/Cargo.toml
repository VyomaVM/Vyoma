[package]
name = "vyoma-agent-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared message types for the Vyoma in-VM agent"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"